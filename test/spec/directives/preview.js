// 'use strict';

// describe('Directive: preview', function () {

//   // load the directive's module
//   beforeEach(module('bulletinStandaloneApp'));

//   var element,
//     scope,
//     text,
//     unPreviewed;

//   beforeEach(inject(function ($rootScope) {
//     scope = $rootScope.$new();
//   }));

//   it('should make hidden element visible', inject(function ($compile) {
//     text = 'this is a really long piece of text and some of it should be cut off'
//     unPreviewed = angular.element('<p class="truncate">' + text + '</p>');

//     expect(unPreviewed.text()).toBe('something');


//     // element = angular.element('<preview>' + text + '</preview>');
//     // element = $compile(element)(scope);
//     // expect(element.text()).toBe('this is the preview directive');
//   }));
// });
